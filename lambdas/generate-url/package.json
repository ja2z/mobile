{
  "name": "lambda-generate-url",
  "version": "1.0.0",
  "description": "Lambda function to generate Sigma embed URLs",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "./build-lambda.sh",
    "package": "npm install && zip -r generateSigmaEmbedURL.zip index.js package.json node_modules",
    "deploy": "npm run package && aws lambda update-function-code --function-name generateSigmaEmbedURL --zip-file fileb://generateSigmaEmbedURL.zip"
  },
  "dependencies": {
    "@aws-sdk/client-secrets-manager": "^3.921.0"
  }
}

