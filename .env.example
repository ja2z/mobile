# Environment Variables for Big Buys Mobile App
# 
# Copy this file to .env.local and customize as needed.
# .env.local is already in .gitignore and won't be committed.

# Magic Link Type Configuration
# - 'direct': Uses custom scheme (bigbuys://auth?token=xxx) - Best for Expo Go testing
# - 'universal': Uses HTTPS redirect (https://mobile.bigbuys.io/auth/verify?token=xxx) - Best for production/TestFlight
# 
# For Expo Go testing, set this to 'direct':
EXPO_PUBLIC_AUTH_LINK_TYPE=direct

# For production builds, either:
# - Remove/rename this file, or
# - Set to 'universal' (or omit this variable - 'universal' is the default)
# EXPO_PUBLIC_AUTH_LINK_TYPE=universal
